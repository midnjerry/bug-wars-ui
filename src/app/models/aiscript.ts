export class AIScript{

    id: number;
    name: string;
    script: string;

    constructor(id:number, name: string, script: string) {
        this.id = id;
        this.name = name;
        this.script = script;
    }
}
